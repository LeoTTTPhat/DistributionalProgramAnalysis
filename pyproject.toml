[project]
name = "distributional-program-analysis"
version = "0.1.0"
description = "Smoke-test prototype for distributional program analysis experiments."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
